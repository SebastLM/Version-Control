will have to store in the server each project so when commiting a file, it knows where it should be inserted the file. the server will first need to verify if the path exists and if not create it. if the path doesnt exist the program will fail to create the file so i need 

need to think more about it, make it more realistic as possible 




add a way to commit to whatever server we want as long as they are running the server code and we know the ip, this will enable for project sharing in different between peers
/*
the server stores the state of their version. for this for each version commited i could just add +1 in each exchange. if the server has a prior commit number, will transfer the project again. if the server doesnt have any, the project doesnt exist and the client will handle that by sending the whole project.
the server will just have to create the dir and then store everything (done by the simple file_receiver).
*/